name: Update BLS Data

on:
  schedule:
    # Run at 10 AM UTC on the first Friday of every month
    - cron: '0 10 1-7 * 5'
  workflow_dispatch:  # Allow manual trigger

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run data collection script
        env:
          BLS_API_KEY: 7b88692e4218dd6d9bc9242deac25484558697cd0f2efd1f9a8b27
        run: |
          python src/collect_data.py
      
      - name: Commit and push if changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/processed/labor_stats.csv
          git diff --quiet && git diff --staged --quiet || (git commit -m "📊 Update BLS data - $(date +'%Y-%m-%d')" && git push)